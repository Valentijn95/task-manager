<h1><%= @task.title%></h1>
<p><%= @task.details%></p>

<%= link_to "back to overview",  tasks_path %>
<br><br>
<%= link_to 'Delete task', task_path(@task.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>

<%= link_to 'Update task', edit_task_path(@task.id) %>

<%= link_to "Finish task",  finish_task_path(@task.id) %>
